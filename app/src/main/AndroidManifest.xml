<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.icloud.ganlensystems.pumitaschool">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
         Test
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".PumitaScoolApp"


        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity
            android:name=".bienvenido"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".registro_alumno_inicio"
            android:label="@string/title_activity_Registro_Alumno"
            android:parentActivityName=".bienvenido">
        </activity>
        <activity android:name=".registro_alumno_final">
        </activity>
        <activity
            android:name=".registro_negocio_inicio"
            android:label="@string/title_activity_Registro_Negocio"
            android:parentActivityName=".bienvenido">
        </activity>
        <activity android:name=".registro_negocio_final">
        </activity>
        <activity
            android:name=".recuperar_password"
            android:label="@string/title_activity_recuperar_password"
            android:parentActivityName=".bienvenido">
        </activity>
        <activity
            android:name=".welcome"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".Image_List"
            android:label="Promociones"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".WS_Negocio"
            android:label="Publica tus promociones"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
             OK
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"/>

        <activity
            android:name=".MapsActivity"
            android:colorMode="@style/Theme.AppCompat"
            android:label="Conoce los mejores lugares"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Widget.AppCompat.Toolbar">
        </activity>
        <activity
            android:name=".MarkerInfo"
            android:label="@string/Mapa"
            android:parentActivityName=".MainActivity">
        </activity>

        <service android:name=".MyFireBaseMessangingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <service android:name=".MyFirebaseInstanceService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

        <activity
            android:name=".FinalRegistroAlumno"
            android:label="Registrate Ahora"
        >
        </activity>
        <activity android:name=".unam_offline"
                  android:label="UNAM"
                  android:parentActivityName=".MainActivity">
        </activity>
        <activity android:name=".WebView"
                  android:label="UNAM"
                  android:parentActivityName=".MainActivity">
        </activity>
        <activity android:name=".LoginActivity"
        android:label="Noticias"
        android:parentActivityName=".MainActivity">
    </activity>
    </application>
</manifest>